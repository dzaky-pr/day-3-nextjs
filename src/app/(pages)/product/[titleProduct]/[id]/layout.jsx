export const metadata = {
  title: 'Detail Product Page | Toko Dzaky',
  description: 'Ini adalah detail product toko dzaky yang keren',
  favicon: '/favicon.ico',
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}
